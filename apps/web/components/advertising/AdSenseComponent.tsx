"use client"

/**
 * Platzhalter: in diesem Projekt aktuell keine Ads.
 * (wird von der portierten `image-compress`-UI referenziert)
 */
export function AdSenseComponent(_props: {
  slotId?: string
  className?: string
  padding?: string
  fluid?: boolean
  maxHeightPx?: number
}) {
  return null
}

