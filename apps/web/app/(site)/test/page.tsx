import { BatchCompressPreview } from "@/components/image-compress/batch-compress-preview"

export const metadata = {
  title: "Test — Batch-Komprimierung Vorschau",
  robots: { index: false, follow: false },
}

export default function TestPage() {
  return <BatchCompressPreview />
}
