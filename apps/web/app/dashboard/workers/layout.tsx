import type { ReactNode } from "react"

export const metadata = {
  title: "Workers | Admin",
}

export default function DashboardWorkersLayout({ children }: { children: ReactNode }) {
  return children
}
